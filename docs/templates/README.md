# Templates

可复用的文档和卡片模板。`docs/00-09` 是固定管线文档；本目录是跨文档引用的公共模板。

## 现有模板

- `asset-reuse-matrix.md` — 资产复用矩阵，供 `docs/06-fusion-strategy.md` 引用
- `evidence-card.md` — 证据卡通用格式，供 `research/*-cards/` 落盘使用

## 添加规则

- 只放"被多个文档引用"的模板；只用一次的结构直接写进目标文档
- 模板必须保留三段式占位（证据/推断/决策）
- 不把半成品判断写进模板默认值
